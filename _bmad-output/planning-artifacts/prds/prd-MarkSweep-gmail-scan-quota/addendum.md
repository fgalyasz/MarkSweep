# Addendum — MarkSweep Gmail scan quota

`ImmediateSleeper` in tests. `TaskSleeper` in the app. `gmailScanPauseSeconds` is 0.15. Retry delays 1s, 2s, 4s (sleeper records them in tests). `scanMessages` returns `ScanOutcome`.
